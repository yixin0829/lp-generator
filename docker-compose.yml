version: '3.9'

services:
  lp-frontend:
    image: lp-frontend
    build: ./client
    ports:
      - 3000:80
  lp-backend:
    image: lp-backend
    build: ./server
    ports:
      - 8000:8000
    env_file:
      - ./server/.env
